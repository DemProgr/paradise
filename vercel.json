{
  "framework": "vite",
  "buildCommand": "vite build --outDir dist --emptyOutDir",
  "outputDirectory": "dist",
  "devCommand": "vite dev",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
