{
  "framework": "vite",
  "build": {
    "command": "vite build --outDir dist --emptyOutDir",
    "env": {}
  },
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
